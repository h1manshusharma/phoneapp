import 'package:flutter/material.dart';

class Voicemail extends StatelessWidget {
  const Voicemail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Voicemail Page")),
    );
  }
}
